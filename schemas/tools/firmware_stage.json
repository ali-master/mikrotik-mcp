{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "firmware_stage",
  "type": "object",
  "properties": {
    "channel": {
      "description": "Set the update channel before downloading (default: keep current).",
      "type": "string",
      "enum": ["stable", "long-term", "testing"]
    }
  },
  "additionalProperties": false
}
