{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "remove_graphing",
  "type": "object",
  "properties": {
    "kind": {
      "type": "string",
      "enum": ["interface", "queue", "resource"]
    },
    "entry_id": {
      "type": "string",
      "description": "RouterOS '.id', e.g. '*1'"
    }
  },
  "required": ["kind", "entry_id"],
  "additionalProperties": false
}
